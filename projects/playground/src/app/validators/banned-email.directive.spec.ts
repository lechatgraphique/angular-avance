import { BannedEmailDirective } from './banned-email.directive';

describe('BannedEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new BannedEmailDirective();
    expect(directive).toBeTruthy();
  });
});
